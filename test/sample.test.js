/* jslint node: true */
/* eslint-env mocha */

const chai = require('chai');

const expect = chai.expect;

describe('sample testing', () => {
  it('should be true', () => {
    expect(true).to.equal(true);
  });
});
